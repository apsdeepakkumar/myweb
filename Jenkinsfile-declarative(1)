pipeline {
    agent {label 'jenkins-slave'}

    stages{
        stage('definition of declarative pipeline')
        {
            steps{
                echo "It's a declarative pipeline"
            }
        }
    }
}
